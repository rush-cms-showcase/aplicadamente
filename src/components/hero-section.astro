---
import IconCheck from '~icons/ph/check-circle-light'
import { Image } from 'astro:assets'
import Container from '@/components/container.astro'

const steps = [
  "Reforço Escolar Individualizado",
  "Apoio ao Desenvolvimento Cognitivo",
  "Acompanhamento Socioemocional"
]
---
<div class="w-full lg:max-h-screen lg:h-screen">
  <Container>
    <div class="w-full lg:h-screen grid lg:grid-cols-2 items-center gap-4 lg:gap-8 max-md:pt-32 max-lg:pt-48">
      <div class="w-full lg:h-full lg:pt-32 flex flex-col justify-center items-center md:items-start text-center lg:text-left z-10 space-y-6 md:space-y-8 max-w-2xl my-auto pb-12">
        <h1 class="text-3xl md:text-4xl lg:text-5xl font-bold text-brand-blue leading-tight tracking-tight">
          Desbloqueie o Potencial do Seu Filho com Acompanhamento Personalizado
        </h1>
        
        <p class="text-base md:text-lg text-slate-600 leading-relaxed md:pr-12">
          As dificuldades escolares podem ser superadas com o suporte certo! Com estratégias neuropsicopedagógicas e reforço escolar personalizado, ajudamo crianças e adolescentes a fortalecerem a aprendizagem, desenvolverem habilidades socioemocionais e ganharem mais confiança. Cada aluno é único, e o aprendizado também deve ser!
        </p>

        <ul class="flex flex-col gap-4 w-full items-center lg:items-start px-4 md:px-0">
          {steps.map(step => (
            <li class="flex items-center gap-3 text-brand-blue font-medium text-[15px] md:text-lg">
              <IconCheck class="w-6 h-6 md:w-7 md:h-7 text-brand-pink shrink-0" />
              <span class="text-left">{step}</span>
            </li>
          ))}
        </ul>
        
        <div class="pt-4 pb-8 md:pb-0 block md:hidden">
          <a 
            href={`https://wa.me/${(import.meta.env.WHATSAPP_NUMBER || '5541999999999').replace(/\D/g, '')}`} 
            class="bg-brand-pink text-white font-bold text-lg py-4 px-8 rounded-full shadow-lg hover:bg-rose-500 hover:-translate-y-1 transition-all duration-300 w-full inline-block text-center"
          >
            Agendar Avaliação
          </a>
        </div>
      </div>

      <div class="w-full lg:h-full flex justify-end items-end relative z-0">
        <Image 
          src="/andressa.png" 
          alt="Andressa Gazzaroli - Foto" 
          class="w-full h-full lg:h-4/5 object-contain object-bottom"
          width={779}
          height={872}
        />
      </div>
    </div>
  </Container>
</div>
